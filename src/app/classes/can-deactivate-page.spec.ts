import { CanDeactivatePage } from './can-deactivate-page';

describe('CanDeactivatePage', () => {
  it('should create an instance', () => {
    expect(new CanDeactivatePage()).toBeTruthy();
  });
});
