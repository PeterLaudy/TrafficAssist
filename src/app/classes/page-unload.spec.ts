import { PageUnload } from './page-unload';

describe('PageUnload', () => {
    /* We can not create an instance of an abstract class.
    it('should create an instance', () => {
        expect(new ClassToTest()).toBeTruthy();
    });
    */
});
