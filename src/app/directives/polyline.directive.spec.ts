import { PolylineDirective } from './polyline.directive';

describe('PolylineDirective', () => {
  it('should create an instance', () => {
    const directive = new PolylineDirective();
    expect(directive).toBeTruthy();
  });
});
